[package]
name = "services"
version = "0.1.0"
edition = "2024"

[dependencies]
variables = { path = "../variables" }
sqlx = { version = "0.7", features = [
    "runtime-tokio-rustls",
    "chrono",
    "json",
    "uuid",
    "sqlite",
] }
